# Testbed

2D-discrete testbed to facilitate HAT-research, with SAIL connection




# Nodejs server:
- In root: `npm init`
- `npm install`
- `cd visualization` + `node server.js`

# nodejs server Todo:
- steal css from SAIL
